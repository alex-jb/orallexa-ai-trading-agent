# Session Checkpoint — 2026-04-24

Resumable state for the Phase 7 + Phase 8 signal-fusion expansion session.

## Branch / HEAD
- Branch: `master`
- Final commit: `ff44295 feat: API + UI + docs + fusion comparison for Phase 7/8 modules`
- Origin: in sync
- CI: all 4 jobs green on every push in this session

## What shipped (26 commits this session)

### Phase 7 — fill out roadmap Phase 5 TODOs
- `skills/social_sentiment.py` (Reddit + optional X)  → 6th fusion source
- `engine/earnings.py` (calendar + PEAD drift)         → 7th fusion source
- `llm/call_logger._send_to_posthog` (LLM Analytics)

### Phase 8 — trending-repo borrowings
- `skills/prediction_markets.py` (Polymarket)           → 8th fusion source
- `engine/portfolio_manager.py` (TradingAgents-style gate)
- `llm/call_logger._send_to_langfuse` (prompt/eval observability)

### Tier-2 additions
- `engine/news_aggregator.py` (Google News + Yahoo RSS)
- `engine/layered_memory.py` (FinMem short/mid/long tiers)
- `engine/regime_strategist.py` (AgentQuant regime→strategy mapping)

### Pipeline integrations (runtime wiring)
- `engine/daily_intel._fetch_headlines` — opt-in RSS via `DAILY_INTEL_USE_RSS=1`
- `llm/perspective_panel.run_perspective_panel` — LayeredMemory narrative injection
- `core/brain.OrallexaBrain.run_for_mode` — Portfolio Manager final gate (opt-in)
- `core/brain.OrallexaBrain.get_regime_strategy` — new method

### Public surface
- `GET /api/regime/{ticker}` — regime + strategy proposal
- `GET /api/layered-memory/{role}/{ticker}` — tier stats
- `/api/deep-analysis` response now surfaces `portfolio_manager` verdict
- `orallexa-ui` new `RegimeCard` component + auto-fetch after deep-analysis

### Review + tooling
- 3 fixes from /review: leap-year (earnings), SSRF guard (Reddit),
  UI enum completeness (SOURCE_LABELS)
- 3 additional fixes from Phase-8 /review: None-confidence crash guard
  in brain, PM exception breadcrumb, Polymarket yes_index bias removal
- `scripts/demo_pipeline_e2e.py` — live fuse_signals → decision → PM
- `scripts/demo_fusion_v2.py` — mocked 7-source ablation study
- `scripts/compare_fusion_variants.py` — 5-src vs 8-src A/B across basket
- Dead-code sweep: 3 unused imports removed (vulture pass)

### Docs
- `docs/NEW_MODULES.md` — full catalog with enablement env vars + APIs
- `CHANGELOG.md` 2026-04-24 entry
- `FURTHER_UPDATES.md` Phase 7 + Phase 8 entries
- `README.md` architecture flow + table updates (5 sources → 8)

## Stats

| Metric | Value |
|--------|-------|
| Commits this session | 26 |
| Net lines changed | ~+6000 / −120 |
| New backend tests | 159 (all green) |
| New frontend tests | 0 (existing 245 still green) |
| CI runs | ~10, all green |
| Signal sources | 5 → 8 |
| New env-gated features | 4 (POSTHOG / LANGFUSE / TWITTER / DAILY_INTEL_USE_RSS) |

## Known follow-ups (not blocking)

1. `skills/technical_analysis_v2.py:74` — ADX calculation has an
   unsatisfiable ternary (`= 0 if False else minus_dm[...]`) that's
   effectively a no-op. Predates this session; probably a real bug in
   the minus_dm zeroing step but changing it affects every strategy.
   Needs a dedicated review + regression run before touching.
2. Walk-forward backtest of the 8-source fusion vs the 5-source legacy
   across a multi-year window. Requires cached historical Polymarket +
   Reddit data we don't yet maintain. `scripts/compare_fusion_variants.py`
   is the current-snapshot substitute; today's run showed NVDA flipping
   BULLISH (+21) → NEUTRAL (+8) after adding the three new sources.
3. `orallexa-ui` `SignalFusionCard` doesn't yet render a breakdown for
   Portfolio Manager warnings/decisions. Data flows to `/api/deep-analysis`
   now, but the UI component needs a sibling card or inline section.
4. CI still runs on Node 20 actions (deprecated June 2026). Non-urgent.

## How to resume

Typical entry points on the next session:

```bash
# Smoke test — will hit real Reddit / yfinance / Polymarket / FinBERT
python scripts/demo_pipeline_e2e.py NVDA TSLA

# Variant comparison
python scripts/compare_fusion_variants.py

# Full test suite (skip the slow RL + E2E-API tests)
python -m pytest --ignore=orallexa-ui --ignore=tests/test_api_e2e.py \
                 --ignore=tests/test_ml_regression.py --deselect tests/test_mirofish.py -q

# Frontend
cd orallexa-ui && npm run dev
```

Module catalog: `docs/NEW_MODULES.md`
Historical context: `FURTHER_UPDATES.md` phases 1-8, `CHANGELOG.md` 2026-04-24.
